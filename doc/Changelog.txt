To v1.42d02
  - naughty bug around line 489 ($CURR{"s_dtrfile"} => $dtrfile)
  - LH2-20020828
  - added p_preadd, p_postdel program possibilities

To v1.42
  - using LH2-20020825 with standard exports instead of global function
    replacements
  - fixed sig handler
  - fixed &getgrent ... routines and their implementation
  - if an error occurs, we print either "ERROR:" or "WARNING:"
  - &assitant: finished it
  - &apply_changes: first creating backups then wait
  - &apply_changes: waiting 2 secs instead of 3 now
  - &apply_changes: users to be added / removed are logged to $proj.log
  - &apply_changes: user first removed, then added
  - &apply_changes: shifted compression type:
    0 => do not backup homedir
    1 => tar
    2 => tar-bzip2 (standard in base/dautool.cfg)
  - &useradd: fixed typo "UPW" to "DTR"
  - &recover: is untested

To v1.41
  - a not-finished bug-fix and was never released

To v1.40
  Startup:
    - changed $CURR{"vgld"} into $CURR{"b_cmpd"}
    - added rootdir / basedir lookup
    - added an option $OPT{"p_predel"} for a script to be executed before the
      user gets deleted (also see $OPT{"p_postadd"} for a script to be executed
      after a particular user has been added)
    - fixed mis-recognition for -c switch
    - removed --config and --nopid (but kept -c and -p)
    - removed check for updates
    - removed platform check
    - remvoed root check
    - removed ITD
    - removed option "save config" in "runtime settings" since never used
    - removed runtime settings menu. never used.
    - removed "manual" menu entry
    - removed workspace climate where possible

  General:
    - doc obsoleted by assistant
    - omit "tables/", put everything into "." (current directory) or
      create a single directory for project
    - changed foreach $_'s into named vars like $f, $l
    - replaced foreach()s with for() or while() -
      reduced memory & processor usage
    - use "Convert" in the mainmenu to get something into DTF (FMT) format
    - squeezed name conversion code into &name2user
    - removed unnecessary workspace hash allocation for &INIT_cpid
    - now using getgrnam() or getgrgid() instead of &group2gid
    - new salt generation algorithm in &genpass
    - fastened the &modext function

  (7)Convert:
    - corrected typo: "Codec::A2A" => "Convert::A2A"
    - but replaced &Convert::A2A::to_ansi with just &to_ansi since exported
    - added missing cleanups when encountering an error in inputfile
      in &convert
    - added recognition for SDF "(* text text text... *)" comment

  (2)Find_Changes:
    - fixed BIG BUG since DAUtool v1.01: if there existed a jengelh1 in PASSWD
      and DTF, we never found it and always created a new jengelh.
      Guess why the presentation of DAUtool failed last year... since of this
      DAMN BUG. Finally fixed.
    - storing users with their realname instead of DTF-Lname

  (3)Apply_Changes:

  (4)Print passowrd lists:
    - now wants a filename to send output to
    - also can operate in standard (regex) lookup mode or reformat the DTR
      class-sorted

  (6)Userdetails:
    - fixed wrong var name near "TO BE DELETED" ($_ => $lname)
    - fixed wrong var name near "for(my $l" ($_ => $l)

[DE] Von v0.80 auf v1.41 (GRAND CHANGES)
  Startup:
    - aufgeräumter Verzeichnisbaum
    - externe Konfigurationsdatei(en) statt in-Programm-Veränderung
    - PID-Lock-System

  Optik:
    - Aussehen verbessert, 1000e von Newlines entfernt (außen sowie im Code)
    - Menüs, alle Programmteile in einem
    - Statusanzeigen
    - korrekte Grammatik (0 ~er, 1 ~, 2 ~er) und keine billigen Multilösung wie
      "1 Datei(en) kopiert"

  (5,7)Formatieren
    - SDF-Datei kann im MS-DOS-Format (ASCII + CRLF) oder im UNIX-Format
      (ANSI + LF) sein
    - Realname -> Loginnamen - Squishing verbessert nach Logik
    - Umlaute / Akzente im Namen werden beibehalten, nur im Kürzel werden sie
      angepasst. Unterstützung für viele andere Akzente des ISO-8859-1.
    - Statt GID-Angabe jetzt (nur) Gruppenname möglich
    - Neue Datumscodierungsroutine (auch Y2k-kompatibel)

  (2)Änderungen finden
    - Header in FMT- und UPW-Dateien (inkl. Versionsnummer)
    - Prüfung der GID zu / von der hinzugefügt / gelöscht werden soll
    - Benutzerindex (z.B. jengelh1) läuft jetzt unendlich weiter, also
      jengel10, jenge100, etc.
    - Vergleichsroutinen gefixt, vereinfacht

  (3)Änderungen zuweisen
    - Warteschleife statt Prompt vor Modifizieren (wie bei cdrecord)
    - kleines Sicherheitsloch für Modifizieren gestopft
    - SID für </home/_trash/>
    - Passwortgenerierungsroutine ist nun immer zufällig
    - Passwortlänge ist variabel bis 8 Buchstaben
    - Benutzer werden nach strengen Sicherheitsmaßnahmen entfernt
    - Kompression bei Archivierung der Homeverzeichnisse zu löschender Benutzer

  (4)Passwortlisten drucken
    - Suchmuster für alle Felder
    - Perl-Regexp-style-Suchmuster
    - Output in Datei oder auf Bildschirm
    - sofort klassensortierte Ausgabe

  (6)Benutzerdetails
    - alle Felder sind suchbar (Muster: &&/L_AND)

  Generell:
    - (ordentliches) Manual geschrieben, für Textconsolen- oder Grafikbrowser
    - alles besser

  Intern:
    - komplett neu geschrieben (u.a. auch %OPT, %__ und %CURR-style)
    - %SIG-Implementation
    - Argumenterfassung besser
    - Defaults bei einigen Prompts
    - ein einziges Programm

To v1.22-post1 [v1.23]
  Mainmenu:
    - fixed typo that lead to crash when exiting DAUtool (so this one did not
      matter at all)

To v1.22
  Startup:
    - "base" dir_struct
    - tables directory squished
    - BEGIN {} now has unshift() instead of push()
    - threw IO::Handle out (speed gain)
    - using Convert::A2A ver 20020609
    - simple use() as initiator
    - using Getopt::Long for parsing parameters
    - multiple configuration files now use this parametric syntax:
      dautool -c config -c config ...
      the comma separated list is not supported any more, and usage will
      result in an attempt to open i.e. "config,thatconfig", and that file
      will not probably exist.
    - new options overall:
        -c FILE  --config FILE  --config=FILE     Configuration file
        -l       --noplat                         Skip arch check
        -p       --nopid                          Skip PID check
        -r       --noroot                         Skip EUID-0 check
        -t       --noitimed                       Do not start ITIMED
    - hmenu() at the end of init()

  General:
    - if()enized result-wanting operations
    - added the new Sort::Version routine for INIT_cpid
    - threw docs out of main prog
    - SBrief v1.09, using Getopt::Long and changed docs
      (use `sbrief -h` for help now); threw IO::Handle out
    - CNVEP v1.05, using Getopt::Long; threw IO::Handle out
    - WebPass v1.03, with ZHTML new things and a bugfix

  (2)Apply_Changes:
    - fixed a bug that lead to "unreadable" FMTs
    - added preallocation of buckets for arrays / hashes
    - some "edged" SAMBA support (??) You could call smbpasswd if needed to

To v1.21
  - internal style change
  - $OPT{"s_saddmkupw"} is now $OPT{"s_saupw"}
  - added coreloader progressbar

To v1.20
  - DAUtool now uses real atomic file locking (secure)
  - optimized, saving lots of RAM during procession
  - optimized line calculating code
  - fixed some typos
  - we need the Fcntl module now (file-locking)
  - ITIMED's pid is written in the /var/log/DAUtool.pid, too...
    I just like it althu it is useless.
  - fixed an RPM package error

To v1.11
  - fixed some fux0r3d A2A and DAUtool-date converting routines
  - moved some data into modules
  - removed unused $OPT{"PROGNL"}
  - added regex options "s" and "o" for faster, complete and accurate
    compiling and recognization
  - removed .taz support (.tar.Z)
  - fixed some rest unquoted hash rv-gets
  - changed easy regexes into substr() things to speed it up
  - DAUtool now comes in these flavors: .tbz2, .rpm
  - extended the CRLF removing
  - release number support in INIT_cupd
  - the exit(1) ran by `dautool -Z` needed to be an exit(0), fixed
  - linux detection construct in INIT_cplat easied
  - grand_changes.txt had remainders of the history package, fixed
  - cnvep updated
  - sbrief updated
  - webpass updated

To v1.10
  - v1.02 shudda have been v1.10, 'cause of thy deep changes, respectively;
    well, now _this_ is v1.10, with some minor changes.
  - got rid of historic packages. they suck.
  - section "runtime settings" could not be called, fixed

To v1.02 Final
  - removed typos, leftovers
  - changed INET file positions
  - enquoted auto-return hash thingies
  - fixed some "/etc/shadow" into $OPT{"f_shadow"} and "/etc/passwd"
    in $OPT{"f_passwd"}
  - DAUtool now frees memory after units
  - added missing s_zch "none" method
  - added missing %WS creation in &INIT_cpid()
  - added enreturns 1
  - deleted unused "eval" in &gid2group() and &group2gid()

To v1.02 Beta 12
  - TTY to fork is also only set when on direct consoles
  - init style reformed
  - added zero-doing boolean vars
  - added $ENV{"PAGER"} onto the pager list
  - added standard termsize
  - if(0) construct replaced thru $OPT{"b_saddmkupw"}
  - ITIMED uses time() instead of the system's uptime now, it is faster now
  - fixed HTML bug
  - added missing documentation for some vars

To v1.02 Beta 11
  - (5)useradd optionally outputs to an UPW, which may be filtered thru
    sbrief then, to use it, change the if(0) occurance in line 607 to
    if(1)
  - some sbrief fixes

To v1.02 Beta 10
  - deleted unused initial $OPT{"f_cfgf"}
  - removed "/bin/cat" from the pager list
  - fixed reaping, better: installed proper reaping for multipids
  - added option for manual generation on startup
  - config files cannot be specified with "+" any more
  - switch for manual creation is "-z", "-Z" will generate and exit
  - chk_plat, chk_root, chk_pid separated to subs
  - ITIMED initialization separated to sub
  - hmenu uses no more $INKEY, but $WS{"INKEY"}
  - doc place is now ./dautool.html instead o' doc/dautool.html
  - ITIMED gets killed by SIGKILL after 3 seconds (on shutdown)
  - removed ITIMED signs alive
  - reformatted config file for dumbs

To v1.02 Beta 9
  - added $DB::fork_TTY for debug issues, and it is only defined if
    $ENV{"DISPLAY"} is not set
  - added $OPT{"s_termsd"}, cumulative into scalar
  - added support for multiple config files:
    dautool -c cfg/bla,cfg/bla2
  - added shortening cfg file names: dautool -c +bla == dautool -c cfg/bla
  - removed leftover line ($CURR{"inetfail"})
  - fixed $OPT{"b_sape"} bug in (8)RuntimeVars
  - fixed INIT_title bug (2nd line was always 80 cols wide, did not use
    $OPT{"s_termwx"})
  - added extreme debug options for ITIMED
  - found the doc bug that caused me to use <pre> instead of <ul>
  - fixed doc HTML bugs
  - sum(some) more doc fixes
  - updated src/res/versort.ph with the bugfix

To v1.02 Beta 8
  "Special XMas Edition 2001"
  - added SNOWMAKER for SE_XMAS_2001! (idea by cthiel)
  - $OPT{"zch"} => $OPT{"s_zch"} fix
  - INIT_cupd: fixed possible 404 in future
  - fixed the VerSort routine that did not catch "v1.02a999 <=> v1.02"
  - added runtime var changing
  - added TOC in DOCumentation
  - filetree: src/oldstuff was 00700, made it 00755 [chmod]
  - filetree: corrected wrong DAUtool v1.02 Beta 7 DIFF
  - filetree/sbrief: fixed diffs, worked

To v1.02 Beta 7
  - INIT:
    - $OPT{PROG*} fixes
    - $OPT{"sape"} added for extra searches
    - added tty size
    - "=" and "-"-bars onscreen are now all as wide as screen (tty size)
    - changed umask so files do not have rwxr-xr-x
    - use of Socket (already in but will first be used in next beta)
    - &INIT_cupd separated
    - OS detection more exact (showing a warning message if not suse-linux)
    - no more parentheses for close();
    - DAUtool (not ITIMED) waits now 2 seconds so ITIMED can print it is alive
    - SIG dupset removed

  HMenu:
    - cosmetic newlines added where necessary
    - HMenu got %WS
    - title of each menu is lit
    - (3)modificate in HMenu is unlit if not available
    - added exec() for restart in HMenu (as "REEXEC")

  (3)Modify:
    - &mkdir instead of mkdir so only directories are not affected of umask
    - SIGs in system kill calls are now numbers "-9" instead of "-KILL"
    - fixed archive extension according to method (bzip2, gzip all had .tgz)

  (6)User details
    - exit out of (6)userdetails possible
    - reformatted output of (6)userdetails menu
    - better handling of edited stuff in (6)userdetails
    - completed (6)userdetails

  - ITIMED:
    - ITIMED's $0 is now "dautool (itimed)"
    - ITIMED also exits when parent exits

  - Documentation:
    - documentation typos fixed; documentation updated (+[6])

  - Other:
    - some == 1 subsituted into > 0 and == 0 into < 1
    - various typos
    - Grand Changes and to-do list are now in <src/changes.txt> and
      <src/todo.txt>. Grand Changes List see below.
    - file tree change: all diffs now have the extension ".diff"
    - file tree change: historic package joined the standard package for more
      devel problem solutions
    - changes.txt update: added all the changes from the diffs Alpha 5 - Alpha
      11
    - TTY can specially be redirected thru:
      dautool </dev/tty9 >/dev/tty9 2>&1
      The BASH on * waits for DAUtool to finish on tty9

To v1.02 Beta 6
  - added multiple archive compression methods (bzip2, gzip, none, compress)
  - to-be-removed-users are printed on demand (thru schueler_4beta.cfg)
  - $OPT{PROG*}-fixes
  - subroutines misspelling fixed dtd2 => dtd21
  - cnvep is now capable of "-i same -o same"
  - gen_vre bug fixed (did miscopy)
  - documentation changes

To v1.02 Beta 5
  - corrected $OPT{c_*} to $OPT{b_*} (for "boolean" instead of "config")
  - added user-given password length
  - cosmetic bugs on update checker
  - genrally fixed update checker (was wrong host and port)
  - $OPT{PROG*}-fixes
  - $OPT{"p_postadd"} fixed
  - single user add::showpassword yes/no? fixed
  - user details :: save and exit - text enhanced depending on situation
  - more documentation changes

To v1.02 Beta 4
  - added an update checker
  - DAUtool needs IO::Socket::INET
  - intended and reformatted code
  - added a version sort routine
  - various documentation changes

To v1.02 Beta 3
  - added $OPT{"p_postadd"} method
  - after adding single user (asking for showing password or not)
  - fixed text browser display bug (>/dev/null, did not work with w3m)
  - various documentation changes

To v1.02 Beta 2
  - added w3m as a pager
  - fixed the salt generation routine
