DAUtool
=======

DESCRIPTION
    DAUtool is a program for synchronizing the </etc/passwd> with a list of
    users. This eliminates the need to add and remove each user separately.

    Has limited support for single users.
    Distributed under the GPL >= v2.0

SYSTEM REQUIREMENTS
    Linux or similar Unix-variants, at least 16 megs o' RAM, Perl v5.6.0
    or later, the modules Fcntl and Getopt::Long.

DOWNLOAD-URL
    http://134.76.13.21:2222/h2/prog.zh?p=dautool

--- German

BESCHREIBUNG
    DAUtool ist ein Programm zur Synchronisation der </etc/passwd> mit einer
    Liste von Usern. Somit entfällt das Hinzufügen und Entefernen jenes
    einzelnen Benutzers.

    Bietet auch limitierten Support für einzelne User.
    DAUtool wird unter der GPL >= v2.0 vertrieben.

SYSTEMANFORDERUNGEN
    Linux oder ähnliche Unix-Varianten, mindestens 16 MB RAM, mindestens
    Perl v5.6.0, die Module Fcntl und Getopt::Long.
